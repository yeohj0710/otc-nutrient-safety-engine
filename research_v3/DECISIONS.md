# research_v3 결정 기록

## D-001 (superseded)

2026-06-18 기준 계획서와 2026-06-01·06-04 초기본이 모두 고함량 영양성분 연구를 명시하므로 이 방향을 유지한다는 이전 결정을 보존한다. 최신 사용자 결정으로 이 결정은 더 이상 활성 연구 범위를 정하지 않는다.

## D-002 (superseded)

국내 다빈도 일반의약품 제품 연구와 원문 계획서의 충돌 및 2026년 7월 13일 전자 승인 기록을 근거로 영양성분 방향을 유지했던 결정을 보존한다. 최신 사용자 결정은 이 승인 기록을 새 OTC 연구의 승인·검토·성과로 재사용하지 않도록 명시하므로 활성 효력이 없다.

## D-003

research_v2의 110개 규칙은 legacy 탐색 자료다. source·locator·사람 원문 검토를 충족하지 않은 규칙은 v3 released로 승격하지 않는다.

## D-004

기준 계획서의 학번 공란과 서명 부재는 원문 상태로 기록한다. 별도 전자 승인 결과와 SHA-256를 `research_v3/approvals`에 보존하며 이를 방향·신원·주장 수준·최종본 승격의 승인 증거로 사용한다.

## D-005

2026-07-14 최신 사용자 결정에 따라 활성 연구 방향을 `korean_otc_product_safety`로 전환한다. 연구 질문은 국내 대표 일반의약품의 제품·성분·함량·복용 조건을 구조화하고, 제품명 기준 중복 성분·동일 약리군·최대용량·복용 간격·연령·질환·병용약 위험을 source/locator와 함께 조회하는 시스템의 개발과 평가다.

## D-006

기존 영양성분 자료는 삭제하거나 새 OTC 성과에 합산하지 않는다. 기존 위치를 유지하면서 `direction_status=superseded`, `reason=actual frequent OTC research direction confirmed by user`로 분리한다. 검색·중복 제거·계보·source/locator·범용 엔진·테스트·공통 UI·문서 검증 코드만 감사 후 재사용할 수 있다.

## D-007

새 OTC 연구에서 사람이 수행하지 않은 전문 판정과 승인을 완료로 기록하지 않는다. 기존 승인 마법사, PRESS 35건, 문헌 118/63건, 근거 326건, 규칙 6건, 시나리오 12건과 성능값은 영양성분 구방향 결과다. 활성 OTC metrics의 초기값으로 사용하지 않는다.

## D-008 (superseded in part)

`release_ready=false`와 goal complete 금지는 유지한다. canonical 승격과 production 배포를 사용자 명시 승인 전 금지했던 부분은 후속 사용자 승인으로 대체되었다.

## D-009

2026-07-14 canonical 승격 승인과 실제 승격을 확인하였다. 이 승격은 블라인드 독립평가 완료를 뜻하지 않는다. 2026-07-15 사용자는 이후 변경을 Git에 push하고 production으로 배포하도록 명시적으로 승인하였다. 배포 승인은 `release_ready`, `complete` 또는 임상 성능 주장 허용으로 해석하지 않는다.

## D-010

신신파스아렉스는 4매 포장이 7×10㎠와 10×14㎠ 규격에 모두 허가되어 포장 수량만으로 단위당 함량을 하나로 결정할 수 없다. 원시 HTML·PDF, 제품 마스터, 성분 마스터와 49개 제품-성분-규격 행은 보존한다. 다만 `analysis_status=excluded`, `exclusion_reason=ambiguous_authorized_package_size`로 기록하고 분석 집합, 사이트 검색·계산 대상과 런타임 미지원 후보 목록에서 제외한다.

## D-011

연구 수치는 데이터 층별로 분리한다. 후보·허가 원문 마스터는 16개 제품, 31개 고유 성분, 106개 제품-성분-규격 행이다. 분석·사이트 집합은 13개 제품, 28개 고유 성분, 47개 계산용 제품-성분 연결이다. 두 층의 수치를 같은 의미의 “제품-성분 연결”로 혼용하지 않는다.

## D-012

2026-07-15 허가 용법·용량 PDF에서 제품별 복용 조건 32개를 구조화하였다. 모든 행은 source, locator와 SHA-256을 확인한 `verified_from_authorization_source` 상태다. 이 조건들은 2026-07-14 외부 약사 규칙 검토 이후 추가되었으므로 별도 약사 검토 완료라고 표시하지 않는다.

## D-013

13개 시나리오의 외부 확인 결과는 `codex_prefilled_external_human_confirmation`이다. 평가자가 Codex 예상 답안을 볼 수 있었으므로 `independent_blinding=false`, `performance_claim_allowed=false`, `independent_evaluation_complete=incomplete`, `complete=false`, `release_ready=false`를 유지한다.

## D-014

2026-07-15 `C:\dev\pharmacy-product-catalog`의 776개 판매 SKU를 비공개 후보 모집단으로 연결하였다. 776건은 Firestore 원본 확인 상태지만 의약품 영역·품목코드·성분·용법과 DUR 공식 매칭은 0건이며 `DATA_GO_KR_SERVICE_KEY` 부재로 `blocked_missing_key`다. 따라서 가격과 전체 원본을 복사하지 않고 최소 파생 후보만 보존한다. 정확 이름 교집합 5 SKU는 기존 분석 제품 4개에 대한 검토 후보이고, fuzzy 2 SKU는 교집합에서 분리한다. 추가 99 SKU·97개 이름도 공식 매칭 전에는 제품 마스터, 런타임, 규칙, 논문 제품 수와 성능 지표에 합산하지 않는다.

## D-015

2026-07-16 최신 정식 카탈로그와 portable v1 계약으로 약학정보원 입력 계층을 갱신하였다. 776건 중 `official_match_status=confirmed` 458건은 연구용 검색·분류·후보 선별에 사용한다. `official_item_seq` 기준 고유 약학정보원 품목은 413개이며, `review_required` 10건, `not_found` 11건, `not_applicable` 297건은 자동 사용하지 않는다.

약학정보원 확인은 식약처 품목 허가 검증을 대체하지 않는다. confirmed 458건도 `mfds_promotion_evidence_complete=false`, `runtime_promotion_allowed=false`로 유지한다. 기존 16개 제품 마스터, 13개 분석·사이트 제품, 15개 released 규칙과 독립평가 지표에는 합산하지 않는다. 판매 가격과 portable `ai_context`는 검색 점수, 안전 제외, 동점 해소와 그룹 키에 사용하지 않는다.

약학정보원 제품 코드와 식약처 `item_sequence`는 서로 다른 식별자 체계다. 정확 공식 품목명·제조사·제형·포장·성분을 대조해 기존 16개 제품 중 4개를 연결했고, 어린이타이레놀현탁액 1개는 제조사 불일치와 식별자 교차표 부재 때문에 충돌로 분리했으며, 11개는 미연결로 남겼다. 이름 유사도만으로 두 제품 체계를 병합하지 않는다.

## D-016

2026-07-27 AM-OTC-001에 따라 v4.0 연구 완료 조건을 AI 맹검 독립평가로 바꾸고 AI 자율 PICOS·PubMed 선별 근거층을 신설한다. 같은 지도교수의 2026-07-20 타 연구 방침을 적용한 연구자 결정이며 이 연구에 대한 지도교수 직접 승인을 뜻하지 않는다.

## D-017

2026-07-27 P2 첫 300행 실측에서 32토큰 생성이 시간 예산을 초과했다. 32행 마이크로배치 시험은 체크포인트를 만들기 전에 종료됐으므로 재개 설정은 16행, 입력 1,280토큰, 출력 20토큰으로 낮춘다. 07:50 종료 전환 규칙에 따라 이번 실행에서는 선별을 더 진행하지 않고 300행 부분 상태를 보고한다.

## D-018

2026-07-27 종료 보고에서는 P2 300/5,724행만 완료된 사실을 논문·지표·Notion·Google Drive에 동일하게 반영한다. P3 AI 참조표준, 규칙엔진 AI 맹검평가와 P4 문헌 locator·사이트 검증은 실행하지 않았으므로 관련 성능값을 `null`로 두고 `complete=false`, `performance_claim_allowed=false`, `release_ready=false`를 유지한다.

## D-019

2026-07-27 08:25 사용자가 시간 제한 때문에 축소하지 말고 계속하라고 지시했다. 08:18 부분 보고서는 중간 기록으로 보존하되 성공 조건으로 사용하지 않는다. P2 커버리지 1.0, P3 AI 참조표준·규칙엔진 AI 맹검평가, P4 근거지도·사이트 빌드와 최종 문서 재생성을 다시 필수 완료 게이트로 적용한다.

## D-020

2026-07-27 로컬 소형 모델(3B)로 수행한 P2 선별 산출물 전량을 폐기한다. retain 225/5,724(3.9%)는 주제를 좁게 겨냥한 코퍼스에서 성립할 수 없는 분포이고, 415행은 모델이 출력 스키마를 지키지 못해 강제 처리된 행이라 판정으로 볼 수 없다. 산출물과 실행 스크립트를 `research_v3/otc/literature/screening_discarded_local3b/`로 격리하고, v4.0 체인의 입력·정답·근거·비교 대상 어디에도 사용하지 않는다. 해당 스크립트만을 대상으로 하던 `tests/research/test_screen_v40_literature_local.py`도 함께 제거한다.

## D-021

2026-07-27 P2 선별의 판정 주체를 에이전트 직접 판정(`screener=agent_direct`)으로 고정한다. 로컬 언어모델 로드, 외부 LLM API 호출, 서브에이전트 위임을 모두 금지한다. `tools/agent_screening.py`는 배치 생성·카드 렌더링·스키마 검증·커버리지 검증·매니페스트 생성만 수행하고 판정에 관여하지 않는다. 배치 크기는 50행이며 전체 5,724행이 115개 배치에 정확히 한 번씩 들어간다.

## D-022

2026-07-27 선별 판정 카드의 제시 형식을 제목 전문, 초록 앞 600자, 게재유형 4개, MeSH 상위 12개로 고정한다. 초록 전문 대신 앞부분을 쓰는 이유는 배경·목적 구간이 주제 적합성 판정을 결정하고 MeSH가 노출·결과 축을 보완하기 때문이다. 이 형식은 P2 전 구간에 동일하게 적용하며 매니페스트 `card_render`에 기록한다. P3-A 참조표준은 초록 전문을 사용하므로 P2와 제시 정보가 다르다.

## D-023

2026-07-27 P3-A 층화 설계를 확정한다. 층화 변수는 P2 근거 형태(`evidence_basis`) × P2 판정(`decision`)의 6개 층이며, 층별 최소 20건(프레임이 20 미만이면 전수)을 배정한 뒤 잔여를 프레임 크기에 비례 배분해 300건을 뽑는다. 시드는 20260727이고 층별 프레임·표본·가중치를 `sample_manifest.json`에 기록한다. 소수층(title_only 계열, uncertain)을 비례 배분만으로 뽑으면 표본이 한 자릿수로 떨어져 층별 추정이 불가능하므로 최소 배정을 둔다. 대신 모든 지표는 층 가중치 `N_h/n_h` 를 적용하고 단순 평균을 쓰지 않는다.

블라인드는 카드에서 P2 판정·신뢰도·이유 코드·배치 ID·record_id·PMID 를 제거하고 라운드별 별칭(`R1-001` 등)만 노출하는 방식으로 구현한다. 별칭↔record_id 대응표는 판정 중 읽지 않고 적재 단계에서만 사용한다.

D-022에서 "P3-A 참조표준은 초록 전문을 사용한다"고 적었으나 실제로는 초록 앞 900자, MeSH 상위 14개를 사용했다. 300건 × 3라운드를 한 번에 읽을 때 도구 출력 한도를 넘겨 판정이 잘리는 문제를 피하기 위한 조정이며, P2(600자·12개)와 다른 제시 형식이라는 점은 유지된다. 실제 사용값은 `render_manifest.json`에 기록한다.

## D-024

2026-07-27 P3-A 종합 라벨은 평가자가 정하지 않고 도구가 코드 규칙으로 도출한다. 규칙은 (1) P·I·O·S 중 하나라도 `N` 이면 `deprioritize`, (2) 그렇지 않고 `I=Y` 이고 `O=Y` 이면 `retain`, (3) 그 외는 `uncertain` 이다. C(비교)는 기록만 하고 라벨에 쓰지 않는다. 노출(I)과 결과(O)가 판정축이고 대상(P)·설계(S)는 거부권만 갖는 구조이며, 주제 적합성을 통째로 한 번에 묻지 않기 위한 설계다.

## D-025

2026-07-27 라운드 3회의 라벨 일치도가 300/300, κ=1.0 으로 나왔다. 이는 동일 평가자가 동일한 명시적 규칙을 재적용했기 때문이며 서로 다른 평가자 간 신뢰도가 아니다. 값이 높다는 이유로 신뢰도 근거처럼 쓰지 않고, 산출물과 논문에 "판정 안정성이지 inter-rater reliability 가 아니다"를 병기한다. 수치를 낮게 보이게 하려고 라운드 설계나 판정을 조정하지 않는다. 동일 에이전트가 P2와 P3-A를 모두 수행했다는 비독립성도 `independence_limitation_ko` 에 남긴다.

## D-026

2026-07-27 AI 참조표준이 `uncertain` 인 45건은 1차 지표 계산에서 제외하고 그 사실과 건수를 함께 보고한다. `uncertain` 을 음성으로 강제하면 특이도가 인위적으로 낮아지고 양성으로 강제하면 민감도가 부풀려지기 때문이다. 대신 `uncertain` 을 음성으로 처리한 민감도 분석을 같은 파일에 병기한다(1차 특이도 0.9047 → 0.8668). 3-way 불일치(`unresolved`)는 0건이다.

## D-027

2026-07-27 P3-B 맹검 카드에 식약처 허가원문 발췌(용법·용량 UD, 사용상의 주의 NB)를 추가한다. 최초 렌더링은 정규화된 허가 상한 4종(`maximum_doses_per_day`, `maximum_units_per_dose`, `maximum_daily_ingredient_amount`, `minimum_interval_hours`)만 실었는데, `administration_constraints.csv` 32행에는 연령 하한과 연속 복용 기간 상한이 아예 없다. 그 상태로 판정하면 `age_restriction` 14건과 `maximum_duration` 14건이 "비교할 기준이 카드에 없음"이라는 이유만으로 전부 `U` 가 되어, 규칙엔진 성능이 아니라 카드 설계의 결함을 측정하게 된다.

발췌 출처는 허가원문뿐이며 `rules.csv`·엔진 설정·규칙 바인딩·심각도는 카드에 넣지 않는다. 규칙표는 평가 대상이므로 참조표준에 넣으면 독립성이 깨진다. 반대로 허가원문은 v4.0에서 결정권을 가진 authority 이므로, 참조표준이 그것을 보고 판정하는 것이 오히려 설계에 맞다. NB 발췌는 규칙 유형별 키워드로 최대 3줄, UD 발췌는 연령 관련 문장 최대 3줄, 각 150자로 자른다.

이 변경으로 카드가 바뀌었으므로 이전 형식으로 수행한 라운드 판정은 전량 폐기하고 3라운드를 처음부터 다시 판정한다. 잠금(`lock`) 전이라 폐기에 제약이 없다. 청크 크기는 120행에서 30행으로 줄인다(라운드당 8청크). 도구 출력 한도를 넘겨 카드가 잘린 채 판정되는 것을 막기 위한 것이다.

## D-028

2026-07-27 `maximum_duration` 은 허가원문에 수치 기준이 없다. 대상 제품(판콜에이내복액)의 NB 는 "장기간 계속 복용하지 말 것"이라고만 적혀 있고 며칠부터인지 나오지 않는다. 따라서 연속 복용 일수를 허가 기준과 비교할 수 없고, 해당 14건의 T 는 `U`(판정 불가)이며 종합 라벨은 `uncertain` 이 된다.

이 결과는 `OTC-RULE-015` 가 `released` 가 아니라 `draft` 인 이유를 참조표준 쪽에서 독립적으로 확인해 준다. 즉 이 규칙은 locator 를 가진 수치 기준이 허가원문에 존재하지 않아 배포될 수 없는 상태이며, 지표에서도 1차 분석에서 빠지고 `draft_rule_analysis` 로 따로 보고한다. 수치를 만들기 위해 임의의 일수(예: 5일)를 가정하지 않는다.

## D-029

2026-07-27 P3-B 사례 생성기의 "미발동 예상" 7건 중 일부는 사용자 조건을 끄는 대신 조건은 그대로 두고 해당 주의가 없다고 가정한 제품으로 바꾸는 방식이다. 그런데 참조표준이 허가원문을 읽고 판정한 결과, 설계가 음성으로 가정한 조합 일부가 양성으로 뒤집혔다. 예: 지르텍정 허가원문에는 "다른 항히스타민제와 마찬가지로 과량의 알코올과 함께 투여하지 않는다"가 있어 `alcohol` 노출이 성립한다. 이 경우 설계 의도가 아니라 허가원문을 따른다. 설계 의도는 정답이 아니며, 참조표준을 설계에 맞추는 순간 독립성이 사라진다. 따라서 규칙 유형별 양성 개수가 정확히 7이 아닐 수 있고, 검증 테스트도 7 고정이 아니라 양·음성이 모두 존재하는지만 확인한다.

## D-030

2026-07-27 P3-B 결과의 해석을 확정한다. released 15개 규칙 유형 210건에서 특이도 1.0000, 정밀도 1.0000, 민감도 0.5702, F1 0.7263, 일치도 0.7667이고 위양성은 0건, 위음성은 49건이다. 이 비대칭은 잡음이 아니라 구조적인 것이다.

민감도가 1.0인 규칙 유형 7개(`age_restriction`, `decongestant_hypertension`, `duplicate_ingredient`, `duplicate_pharmacologic_class`, `max_daily_dose`, `minimum_interval`, `urgent_referral`)는 성분·수량 계산이나 단일 앵커 제품으로 판정이 끝나는 유형이다. 반대로 위음성이 몰린 8개 유형(`pregnancy_lactation` 9건, `sedative_medication` 7건, `anticoagulant_antiplatelet`·`gi_bleeding_ulcer`·`renal_disease` 각 6건, `alcohol`·`hepatic_disease`·`sedation_driving` 각 5건)은 모두 규칙이 앵커 제품 한 개에만 묶여 있는 유형이다. 같은 주의가 허가원문에 적힌 다른 제품(낙센정·덱스피드·판피린티정·지르텍정·어린이타이레놀현탁액·소화효소제 등)에서는 규칙이 발동하지 않는다.

즉 v4.0 엔진은 "허가 근거 없이 경고하지 않는다"는 성질은 완전히 지키지만, "허가 근거가 있는데도 경고하지 않는" 커버리지 공백이 있다. 이것을 성능 실패가 아니라 규칙 바인딩 범위의 문제로 보고하며, 제품×규칙유형 단위 공백 목록을 `coverage_gap_analysis` 에 남겨 P4의 근거 연결 대상으로 넘긴다. 민감도를 올리려고 참조표준을 느슨하게 고치지 않는다.

## D-031

2026-07-27 맹검 훼손 2건(IND-OTC-001, IND-OTC-002)을 모든 1차 지표에서 제외한다. 사례 생성 단계에서 `independent_scenarios.csv` 앞 2행을 확인하다가 그 행의 `human_reference_label` 과 기존 `prediction` 이 평가자에게 노출됐다. 이후 잠금 전까지 해당 파일을 다시 열지 않았고 판정은 카드만 보고 수행했지만, 노출 사실 자체는 되돌릴 수 없다. 잠금 파일에 `blinding_compromised` 플래그로 남기고 산출물에서 사유와 함께 별도 보고한다. 그 결과 legacy 재평가는 13건이 아니라 11건으로 계산된다.

## D-032

2026-07-27 P3-C 상태 갱신을 완료한다. 플래그와 근거 파일은 `research_v3/otc/audit/completion_audit.json` 의 `status_flags` 에 함께 기록한다.

- `independent_blinding_ai=true`, `independent_evaluation_ai_complete=true`, `performance_claim_allowed=true`, `complete=true` — 근거는 `research_v3/otc/validation/ai_independent_evaluation.json`
- `independent_blinding=false` — 사람 블라인드 평가는 수행되지 않았다. 이 플래그는 사람 평가 전용이며 AI 평가로 채우지 않는다
- `release_ready=false` — 임상 배포 승인 절차가 없으므로 유지

`performance_claim_allowed=true` 는 무조건적 허용이 아니라 "AI 참조표준 대비 지표이고 평가자가 사람이 아니라는 사실을 항상 병기한다"는 조건부 허용이며, 그 조건문을 플래그 옆에 함께 저장한다. 완료 판정 로직도 사람 평가 파일이 아니라 AI 평가 파일에서 읽도록 `audit_completion.py` 를 고쳤고, 잠금 해시 일치와 잠금 시각 < 예측 시각을 다시 확인한 뒤에만 `achieved` 가 되도록 했다.

메트릭 매니페스트에는 `sensitivity_vs_ai_reference` 처럼 출처를 이름에 박은 키로 추가하고, 기존 사람 축(`independent_scenarios`)은 그대로 둔 채 합산하지 않는다. 배포 블로커는 0건이 됐지만 평가자 독립성 한계는 `data_limitations` 에 남긴다.

`project_identity.json` 의 `released_rule_count` 를 0에서 15로 고쳤다. 값의 출처(`rules.csv` 의 status=released)도 같은 파일에 적어 다음에 다시 어긋나지 않게 했다.

`import_health_kr_catalog.py` 의 기존 연구 경계 상수에서 `complete` 기대값을 False에서 True로 바꿨다. 이 상수는 카탈로그 가져오기가 기존 연구 상태를 오염시키지 않았는지 확인하는 용도이므로 실제 상태를 따라가야 한다. `independent_blinding` 과 `performance_claim_allowed` 는 사람 평가 파일에서 읽으므로 False 그대로 둔다.

## D-033

2026-07-27 P4 문헌 근거 연결의 출처를 v4.0 검색 코퍼스로 한정한다. 기존 `supporting_literature.csv` 13행 중 v4.0 코퍼스(5,724행)에 들어 있는 것은 2행뿐이었고 나머지 11행은 체계적 검색 밖에서 큐레이션된 것이다. 이 11행을 v4.0 근거지도에 그대로 쓰면 "retain 문헌으로 연결했다"는 서술이 사실이 아니게 된다.

기존 13행은 `research_v3/otc/rules/supporting_literature_pre_v40.csv` 로 그대로 보존하고, `supporting_literature.csv` 는 v4.0 코퍼스에서 retain 판정을 받은 문헌만으로 다시 만든다. 파일 구조도 논문 1편당 1행에서 **규칙 1건 × 논문 1편 = 1행**으로 바꿔, 같은 논문이 두 규칙에 붙을 때 각각 다른 문장을 locator 로 가질 수 있게 했다. 결과는 규칙 16개 전부 연결, 링크 20건, 고유 논문 19편이다.

## D-034

2026-07-27 문헌 locator 를 "초록 문장 인덱스 + 원문 인용" 쌍으로 고정한다(`abstract:sentence:N`). 문장 분할 규칙은 `tools/literature_locator.py` 한 곳에만 두고, 빌더가 매번 `evidence_map.csv` 의 초록에서 해당 인덱스 문장을 다시 뽑아 CSV 의 인용문과 글자 단위로 비교한다. 어긋나면 빌드가 실패한다. 인용문만 적으면 나중에 초록이 갱신됐을 때 어긋난 줄 모르고, 인덱스만 적으면 사람이 검증할 수 없기 때문에 둘 다 저장한다.

`vs.` 같은 축약형에서 문장이 잘리는 문제가 있어 분할기에 예외 목록을 뒀다. 이 규칙을 바꾸면 기존 locator 인덱스가 전부 어긋나므로 변경 시 링크 검증을 반드시 다시 돌린다.

## D-035

2026-07-27 허가원문과 문헌이 어긋나는 4건을 `conflict` 로 보존한다. 지우거나 한쪽으로 맞추지 않는다.

- `OTC-RULE-005`(연령) × PMID 41803773: 소아 투약 오류가 간독성·사망으로 이어지는 경우는 드물다는 결론이 연령 기준의 심각도보다 낮다
- `OTC-RULE-006`(임신·수유) × PMID 39714827: 형제 비교에서는 위험 증가가 뚜렷하지 않았다고 적어 임신 말기 제한을 완화하는 방향으로 읽힐 수 있다
- `OTC-RULE-007`(간질환) × PMID 26460177: 과소용량이 오히려 효과를 못 내게 할 수 있다며 무조건적 제한에 이의를 단다
- `OTC-RULE-010`(졸림·운전) × PMID 26999510: 2세대 항히스타민의 운전 수행 저하를 관찰하지 못했는데 지르텍정 허가원문은 졸음 가능성과 운전 시 주의를 적는다

네 건 모두 판정은 허가원문을 따르고, 화면에는 "허가원문과 다른 점"으로 함께 표시한다. 문헌이 규칙을 배포시키거나 완화하지 못하도록 `supports_rule_release` 는 전부 false 이고 `evidence_authority` 는 `literature_explanatory_only` 다.

## D-036

2026-07-27 제품 13개 중 5개(소화효소제 4종, 제일쿨파프)에는 문헌을 연결하지 않는다. v4.0 코퍼스의 소화기 질문(OTC-LIT-Q04)에서 retain 이 27건뿐이고 그중 국내 일반의약품 소화효소제 조합을 다룬 문헌이 없었다. 맞지 않는 문헌을 억지로 붙이는 대신 공백으로 남기고 그 사유를 `literature_link_manifest.json` 에 적는다. 이 제품들의 판정은 허가원문만으로 이뤄지며 판정 품질에는 영향이 없다.

## D-037

2026-07-27 `capture_software_validation.py` 의 두 가지 결함을 고쳤다. (1) vitest 출력의 ANSI 색상 코드 때문에 앱 시험 개수 정규식이 항상 실패해 매니페스트에 직전 실행의 낡은 값(69)이 남아 있었다. 숫자를 뽑기 전에 색상 코드를 제거한다. (2) 검증 실행이 끝난 뒤 메트릭 매니페스트를 다시 굽지 않아 매니페스트가 항상 한 회차 뒤처졌다. 실행 종료 후 `build_metrics.py` 를 다시 호출한다. 두 결함 모두 문서-매니페스트 일치 검사를 통과시키기 위한 조정이 아니라, 기록된 수치가 실제 실행과 달랐던 문제다.

## D-038

2026-07-27 P5 문서 재생성 도구를 v4.0 완료 상태로 다시 썼다. `build_v40_reporting.py` 의 `collect_metrics` 가 이제 PICOS·코퍼스·선별 매니페스트에 더해 AI 참조표준, 규칙엔진 맹검평가, 문헌 링크 매니페스트, 종료 감사까지 읽는다. 논문 본문·발표 원고·Notion 원고·README·AGENTS·project_map 의 모든 수치는 이 함수를 거치며, 문서에 숫자를 직접 적지 않는다.

폐기한 실행에 대한 서술을 전부 걷어냈다. 이전 원고는 "로컬 모델로 300행을 판정했다"는 문장을 담고 있었고 `docs/project_map.md` 는 이미 격리된 스크립트를 가리키고 있었다. 대신 판정 주체를 `agent_direct` 로, 사람 판정 0건과 지역 모델·외부 API·서브에이전트 미사용을 명시한다. 비교 절은 만들지 않는다.

논문에서 "민감도"를 단독으로 쓰지 않는다. 표 항목과 본문 모두 "AI 참조표준 대비 민감도"로 적고, 초록 첫머리 주의 상자에 이 수치가 사람 참조표준이 아니라는 사실을 먼저 못 박는다.

## D-039

2026-07-27 논문 PDF 10쪽을 전부 이미지로 렌더해 육안 검사했다. 잘림·고아 행·빈 페이지는 없었고, 표는 페이지 경계에서 행이 쪼개지지 않았다. PDF에 포함되지 않은 글꼴은 0개이고 Pretendard 계열 6종이 서브셋으로 embed 됐다. 렌더 이미지는 `research_v3/otc/etc/document_qa/thesis_v40_pages/` 에 남긴다.

## D-040

2026-07-27 Google Drive 최종 동기화를 실제로 수행했다. 13개 파일을 복사하고 원본·사본 SHA-256 을 전부 대조해 일치를 확인했다. 동기화 코드는 파일명에 `_v3백업` 이 들어가면 아예 복사를 시도하지 않고, `90_legacy_구버전_열지말것` 하위 경로도 차단한다. 기존 백업 3개(논문 DOCX·PDF, metrics_manifest)가 그대로 있는지도 보고서에 함께 기록한다.

## D-041

2026-07-27 Notion 페이지 상단 "현재 상태" 절을 최종 상태로 교체했다. 교체 전 상단에는 "300/5,724행에서 중단"이라는 부분 실행 상태가 올라가 있었다. MCP `update_content` 로 해당 절만 바꾸고 `[대체됨]` 절 5개와 하위 페이지 4개는 건드리지 않았으며, 교체 후 페이지를 다시 읽어 반영을 확인했다. 실행 보고서의 `notion_updated` 는 추정이 아니라 이 확인 결과다.

## D-042

2026-07-27 `build_v40_run_report.py` 를 하드코딩 제거 방향으로 다시 썼다. 이전 버전은 단계별 시각, 동기화 파일 목록, 성능 null 값이 전부 코드에 박혀 있었다. 이제 모든 값을 산출물에서 읽고, 동기화는 실제 복사와 해시 대조 결과를 그대로 기록한다. 필수 절 15개(phases, picos, corpus, screening, ai_reference, blind_eval, state_flags, rule_evidence, site, thesis, notion_updated, files_synced, closure, scope_reductions, unresolved)를 모두 채웠고, 추정하지 않은 값만 null 로 남긴다.
